{
  "name": "va-web-clipupload",
  "version": "2.0.0",
  "description": "VillainArc secure video sharing microservice with React frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "client:build": "cd client && npm run build",
    "build": "npm run client:build",
    "deploy": "node deploy.js",
    "heroku-postbuild": "npm run build",
    "railway-postbuild": "npm run build",
    "render-postbuild": "npm run build"
  },
  "keywords": [
    "video",
    "sharing",
    "microservice",
    "villainarc",
    "clip",
    "upload"
  ],
  "author": "VillainArc Guild",
  "license": "MIT",
  "dependencies": {
    "cloudinary": "^2.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0",
    "multer": "^2.0.1",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
